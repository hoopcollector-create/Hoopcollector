import { createClient } from '@supabase/supabase-js'; const supabase = createClient('https://xmfjvhhchhyeobrydasp.supabase.co', 'sb_publishable_hMPDwuiJrwXimYb56p_8Lg_cG1bcA9o'); async function test(){ const { data: { session } } = await supabase.auth.getSession(); if (!session) { console.log('No session'); return; } const { data } = await supabase.from('user_roles').select('*').eq('user_id', session.user.id); console.log('Roles:', data); const { data: profile } = await supabase.from('coach_profiles').select('*').eq('user_id', session.user.id).maybeSingle(); console.log('Coach Profile:', profile); } test();  
