import { createClient } from '@supabase/supabase-js'; const supabase = createClient('https://xmfjvhhchhyeobrydasp.supabase.co', 'sb_publishable_hMPDwuiJrwXimYb56p_8Lg_cG1bcA9o'); async function test(){ const { data, error } = await supabase.rpc('get_table_columns', { table_name: 'coach_slots' }); if (error) { const { data: d2 } = await supabase.from('coach_slots').select('*').limit(0); console.log('Try select limit 0:', d2); } console.log('Data:', data); } test();  
